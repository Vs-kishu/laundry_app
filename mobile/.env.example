# Only needed for production builds or when auto-detection fails.
# In development the app finds your backend automatically (same IP as `expo start`, port 5000).
# EXPO_PUBLIC_API_URL=https://api.yourdomain.com/api
