export const metadata = {
  title: "Log in",
  description: "Log in to Laundry Point to book a pickup or track your order.",
  robots: { index: false, follow: true },
  alternates: { canonical: "/login" },
};

export default function Layout({ children }) {
  return children;
}
